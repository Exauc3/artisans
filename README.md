
  # Prototype Application Artisans-Clients

  This is a code bundle for Prototype Application Artisans-Clients. The original project is available at https://www.figma.com/design/o4bzGPDelOBaiU1R8TXvZm/Prototype-Application-Artisans-Clients.

  ## Running the code

  Run `npm i` to install the dependencies.

  Run `npm run dev` to start the development server.
  